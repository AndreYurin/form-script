export enum NoticeStatus {
  New = "new",
  DetailsCollected = "details_collected",
  Rejected = "rejected",
  Error = "error",
}

export enum ScriptRunStatus {
  Running = "running",
  Success = "success",
  Error = "error",
}
